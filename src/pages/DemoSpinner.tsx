import { Spinner } from "@/components/ui/spinner-1";

export default function DemoSpinner() {
  return (
    <div className="flex items-center justify-center h-screen">
      <Spinner size={50} />
    </div>
  );
}